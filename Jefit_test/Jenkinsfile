pipeline {
    agent any      
    
    stages {       
            stage('Test') {
            steps 
            {
                 echo 'dotnet test --filter Name~"OpenStaptPageTest"'
            }
        }
        
    }
}
